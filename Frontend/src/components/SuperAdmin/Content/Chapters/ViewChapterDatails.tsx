import React from 'react';
import { CreateCardBody, CreateCardFooter, CreateCardHeader, CreateMainCard } from '../../CreateFormstyles';

const ViewChapterDatails: React.FC = () => {
  return (
    <CreateMainCard className='view_card_wrapper'>
      <CreateCardHeader>

      </CreateCardHeader>
      <CreateCardBody>

      </CreateCardBody>
      <CreateCardFooter>

      </CreateCardFooter>
    </CreateMainCard>
  );
};

export default ViewChapterDatails; 
